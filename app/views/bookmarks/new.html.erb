<h1>Add a New Bookmark to <strong><%= @list.name %></strong></h1>

<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.input :movie_id, collection: @movies.map{ |movie| [movie.title, movie.id] }  %>
  <%= f.submit "Submit", class: "btn btn-primary" %>
<% end %>